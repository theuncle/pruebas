//Flickity (Carousel)
$('.main-carousel').flickity({
      // options
      cellAlign: 'center',
      adaptiveHeight: true,
      initialIndex: 1,
      resize: false,
      wrapAround: true
      //autoPlay: true
    });